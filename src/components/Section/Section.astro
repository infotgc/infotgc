---
interface Props {
  class?: string;
  id?: string;
}

const { class: className, id } = Astro.props;
---

<section id={id} class={className}>
  <div class="container">
    <slot />
  </div>
</section>